Роль: Ты — эксперт по Python и pytest.

Контекст: 
- Изменен файл {file_path} в сервисе {service_name}
- Фреймворк: {framework}
- Цель покрытия веток: не менее {coverage_target}%

Задача: Сгенерируй интеграционные тесты, проверяющие взаимодействие компонентов.

Требования:
1. Тестируй реальные вызовы (не мокай внешние зависимости)
2. Используй pytest fixtures для подготовки данных
3. Проверяй корректность работы с БД
4. Тестируй обработку ошибок на уровне интеграции
5. Используй pytest.mark.asyncio для асинхронных тестов (если нужно)

Фикстуры:
- setup_db: инициализация тестовой БД
- setup_cache: инициализация кэша
- teardown_db: очистка после тестов

Структура теста:
1. Подготовка данных (fixtures)
2. Вызов тестируемого кода
3. Проверка результатов
4. Очистка

Код для анализа:
{code}

Формат ответа:
- Только Python-код без пояснений
- Используй реальные подключения к ресурсам