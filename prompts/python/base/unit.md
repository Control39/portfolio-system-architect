Роль: Ты — эксперт по Python и pytest.

Контекст: 
- Изменен файл {file_path} в сервисе {service_name}
- Фреймворк: {framework}
- Цель покрытия веток: не менее {coverage_target}%

Задача: Сгенерируй набор юнит-тестов для функций и классов в этом файле.

Требования:
1. Тестируй happy path (успешные сценарии)
2. Тестируй обработку ошибок (ValueError, KeyError, TypeError, etc.)
3. Используй моки (pytest-mock) для изоляции внешних зависимостей
4. Называй функции тестов понятно: test_{function_name}_{scenario}
5. Каждый тест должен проверять одну конкретную функциональность
6. Используй параметризованные тесты для проверки множества входных данных

Структура файла:
- Импорты (pytest, pytest-mock, модули из {service_name})
- Фикстуры (если нужны)
- Тесты функций и классов

Код для анализа:
{code}

Формат ответа:
- Только Python-код без пояснений
- Код должен быть готов к выполнению через pytest
- Добавь docstring к каждому тесту