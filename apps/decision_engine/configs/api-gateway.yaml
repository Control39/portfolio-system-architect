# apps/decision_engine/configs/api-gateway.yaml
service:
  name: decision_engine
  version: "1.0.0"

model:
  provider: openai
  model_name: gpt-4
  temperature: 0.7
  max_tokens: 2048

rag:
  enabled: true
  index_path: ./data/rag-index
  top_k: 5

logging:
  level: INFO
  format: json
