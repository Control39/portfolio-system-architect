site:
  # SourceCraft Sites configuration - deploys static site from gh-pages branch
  root: "."
  ref: "gh-pages"
  # Optional: base_path for subdirectory deployment
  # base_path: "/portfolio-system-architect/"