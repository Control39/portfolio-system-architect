[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["python_server/tests"]
markers = [
    "timeout: mark tests with timeout"
]

[project]
name = "portfolio-system-architect"
version = "0.1.0"
description = "Backend for portfolio system architect"
readme = "README.md"
requires-python = ">=3.10"
