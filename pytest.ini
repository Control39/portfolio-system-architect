[pytest]
addopts = --cov=apps --cov=src --cov-fail-under=90 --cov-report=html --cov-report=term-missing -m "not slow"
testpaths = apps tests/e2e tests/unit
pythonpath = apps src .
markers =
    e2e: E2E tests (services up)
    slow: Slow tests
