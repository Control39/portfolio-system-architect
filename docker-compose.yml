services:
  it-compass:
    build: ./apps/it-compass
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONPATH=/app

  cloud-reason:
    build: ./apps/cloud-reason
    ports:
      - "8001:8001"
    volumes:
      - ./logs:/app/logs
      - ./apps/cloud-reason:/app
    environment:
      - PYTHONPATH=/app
    depends_on:
      - db

  career-development:
    build: ./apps/career-development/career-development-system
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONPATH=/app

  ml-model-registry:
    build:
      context: ./apps/ml-model-registry
      args:
        - BUILDPLATFORM=linux/amd64
    deploy:
      resources:
        limits:
          memory: 2G
    ports:
      - "8002:8001"
    volumes:
      - ./data/ml-models:/app/models
    environment:
      - DATABASE_URL=sqlite:////app/models/registry.db

  arch-compass:
    build: ./apps/arch-compass-framework
    volumes:
      - ./apps/arch-compass-framework:/workspace
    working_dir: /workspace
    command: pwsh -Command 'Import-Module ./ArchCompass.psm1; Get-Help ArchCompass'

  portfolio-organizer:
    build: ./apps/portfolio-organizer/portfolio-organizer
    ports:
      - "8004:8004"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONPATH=/app

  system-proof:
    build: ./apps/system-proof
    ports:
      - "8003:8003"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONPATH=/app
    depends_on:
      - db

  db:
    image: nouchka/sqlite3:latest
    volumes:
      - ./data/db:/data
    command: tail -f /dev/null

volumes:
  data:
  logs:
