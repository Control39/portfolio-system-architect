# config/defaults/default_config.yaml

version: "1.0.0"
environment: "default"

openai:
  api_key: ""
  model: "gpt-4"
  temperature: 0.7

logging:
  level: "Info"
  output: "Console"

security:
  enable_scanning: true
  vault_type: "Environment"
