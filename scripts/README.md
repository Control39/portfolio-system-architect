# Скрипты автоматизации

> **Назначение:** Утилиты для разработки, тестирования, мониторинга и поддержки качества кода.

## Список скриптов

| Скрипт | Назначение | Команда |
|--------|------------|---------|
| [`check_ports.py`](./check_ports.py) | Проверка согласованности портов | `python scripts/check_ports.py` |
| [`update-coverage-badge.py`](./update-coverage-badge.py) | Обновление бейджа покрытия | `make update-badge` |

---

## `check_ports.py`

**Проверка согласованности портов сервисов между `docker-compose.yml` и `apps/README.md`.**

### Назначение

Скрипт решает проблему «разъезжавшейся документации» — когда порты в README не соответствуют реальной конфигурации Docker.

### Что делает скрипт

1. **Парсит `docker-compose.yml`** — извлекает порты сервисов из Traefik labels
2. **Парсит `apps/README.md`** — извлекает порты из таблицы сервисов
3. **Сравнивает** — находит расхождения и пропуски
4. **Сообщает** — выводит отчёт в терминале
5. **(Опционально)** Падает с кодом 1 для CI/CD (`--strict`)

### Использование

#### Нормальный режим

```bash
python scripts/check_ports.py
```

**Пример вывода:**
```
============================================================
🔍 Проверка согласованности портов
============================================================
📊 Найдено 7 сервисов с портами в docker-compose.yml:
   auth_service: 8100
   decision_engine: 8001
   ...

✅ Согласовано (7 сервисов):
   • auth_service: port 8100
   • decision_engine: port 8001
   ...

============================================================
✅ Проверка пройдена
============================================================
```

#### Режим CI/CD (строго)

```bash
python scripts/check_ports.py --strict
```

- Если есть расхождения → скрипт падает с кодом `1`
- Если всё ок → код `0`

### Нормализация имён

Скрипт автоматически сопоставляет имена:
- `career_dev` → `career_development`
- `ml_registry` → `ml_model_registry`
- `portfolio_org` → `portfolio_organizer`

### Встраивание в CI/CD

Добавьте в `.github/workflows/ci.yml`:

```yaml
- name: Check port consistency
  run: python scripts/check_ports.py --strict
```

---

## `update-coverage-badge.py`

### Что делает скрипт

1. **Запускает тесты** с генерацией `coverage.xml`
2. **Извлекает процент покрытия** из XML-отчёта
3. **Обновляет бейдж** в README.md
4. **Обновляет дату** в `docs/TEST-COVERAGE-METRICS.md`
5. **(Опционально)** Делает git commit

### Использование

#### Через Make (рекомендуется)

```bash
make update-badge
```

#### Вручную

```bash
# Windows / Python
python scripts/update-coverage-badge.py

# Linux / Mac / WSL
python scripts/update-coverage-badge.py
```

### Исключаемые тесты

Скрипт автоматически исключает пре-existing проблемы (не блокируют функциональность):

- `tests/unit/test_assistant_orchestrator_main.py` (CLI-тесты)
- `tests/unit/test_embedding_agent_updated.py` (ChromaDB/HF-тесты)

📄 **Подробности:** [`docs/KNOWN_ISSUES.md`](../docs/KNOWN_ISSUES.md)

### Порог качества

- **Минимальное покрытие:** 90%
- Если покрытие ниже — скрипт прерывается с ошибкой

### Вывод

После успешного выполнения:

```
============================================================
✅ Обновление завершено!
   Покрытие: 93%
   README.md обновлён
   TEST-COVERAGE-METRICS.md обновлён
============================================================
```

### Пример обновления README

**До:**
```markdown
[![Coverage](https://img.shields.io/codecov/c/github/Control39/portfolio-system-architect...)](https://app.codecov.io/...)
```

**После:**
```markdown
[![Coverage](https://img.shields.io/badge/coverage-93%25-brightgreen)](docs/TEST-COVERAGE-METRICS.md)
*Обновлено: 3 мая 2026*
```

### Преимущества

✅ **Прозрачность** — дата обновления всегда актуальна
✅ **Автоматизация** — не нужно вручную считать покрытие
✅ **Защита от лжи** — бейдж обновляется только ПОСЛЕ успешных тестов
✅ **Работает из РФ** — статический бейдж не блокируется

### Частые вопросы

**Q:** Что если тесты упали?
**A:** Скрипт прервётся с ошибкой. Бейдж не будет обновлён.

**Q:** Можно ли изменить порог покрытия?
**A:** Да, в строке `--cov-fail-under=90` замените 90 на нужное значение.

**Q:** Как добавить новые исключаемые тесты?
**A:** Отредактируйте список `EXCLUDED_TESTS` в скрипте.

---

*Создан: 3 мая 2026 г.*
