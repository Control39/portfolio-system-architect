apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: portfolio-system

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - configmap.yaml

commonLabels:
  component: streamlit-ui
  app: ai-architect
