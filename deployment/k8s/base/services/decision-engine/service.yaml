apiVersion: v1
kind: Service
metadata:
  name: decision-engine
  namespace: portfolio
spec:
  type: ClusterIP
  selector:
    app: decision-engine
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
