apiVersion: v1
kind: Service
metadata:
  name: ml-model-registry
  namespace: portfolio
spec:
  type: ClusterIP
  selector:
    app: ml-model-registry
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
