# Monitoring (Prometheus + Grafana)

## Как запустить
```bash
docker compose -f docker-compose.yml -f docker-compose.monitoring.yml up -d
```

## Доступ
- **Prometheus UI** – http://localhost:9090  
- **Grafana UI** – http://localhost:3000 (admin
