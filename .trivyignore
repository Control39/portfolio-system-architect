# Trivy Ignore File
# Исключение известных уязвимостей и ложных срабатываний

# ============================================
# ОСНОВНЫЕ ИСКЛЮЧЕНИЯ (по причине)
# ============================================

# Dev-зависимости (не в production)
CVE-2024-12345  # dev-only: тестовые фреймворки
CVE-2024-23456  # dev-only: линтеры и форматтеры

# Устаревшие базовые образы Docker (планируется обновление)
CVE-2024-34567  # python:3.11-slim base image - planned Q3 2026
CVE-2024-45678  # python:3.11-slim base image - planned Q3 2026

# False positives (не влияют на проект)
CVE-2024-56789  # False positive: уязвимость в необрабатываемом коде
CVE-2024-67890  # False positive: уязвимость в необрабатываемом коде

# ============================================
# ИСКЛЮЧЕНИЯ ПО ПРИЧАСТНОСТИ (ACCEPTED RISK)
# ============================================

# Эти уязвимости признаны приемлемым риском:
# - Не используются в production (dev dependencies)
# - Требуют физический доступ для эксплуатации
# - Исправление сломает совместимость (планируется в следующей мажорке)

# ============================================
# ФОРМАТ: CVE-ID # причина
# ============================================
# Чтобы добавить новое исключение:
# 1. Убедись, что уязвимость не критична для production
# 2. Укажи причину (dev-only / false-positive / accepted-risk)
# 3. Укажи план исправления (если применимо)
