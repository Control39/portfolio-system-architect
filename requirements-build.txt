# ============================================================================= 
# BUILD DEPENDENCIES
# =============================================================================
# Требуются для выполнения `pip install -e .`
# Не включают runtime-зависимости (те — в requirements.txt или requirements-dev.txt)

setuptools>=69.0.0
wheel>=0.40.0
pkginfo>=1.9.0

# Если используете pyproject.toml с современным бэкендом
# build>=0.10.0